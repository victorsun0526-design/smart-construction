# 🌐 智慧工地平台 - 网站部署指南

## 🎯 部署目标
将本地智慧工地平台部署为可通过互联网访问的网站，支持：
1. 远程登录访问
2. 多用户同时使用
3. 数据安全存储
4. 稳定可靠运行

## 📋 部署方案对比

### 方案一：云服务器部署（推荐）
**适合**：正式商用，需要完整控制
**成本**：500-2000元/月
**复杂度**：中等
**推荐平台**：阿里云、腾讯云、华为云

### 方案二：静态网站托管
**适合**：演示测试，快速上线
**成本**：0-100元/月
**复杂度**：简单
**推荐平台**：Vercel、Netlify、GitHub Pages

### 方案三：容器化部署
**适合**：技术团队，需要弹性扩展
**成本**：300-1500元/月
**复杂度**：较高
**推荐平台**：Docker + Kubernetes

### 方案四：全托管SaaS平台
**适合**：无技术团队，快速上线
**成本**：1000-5000元/月
**复杂度**：简单
**推荐平台**：Render、Railway、Fly.io

## 🚀 推荐方案：云服务器部署（阿里云）

### 第一步：购买云服务器（1小时）

#### 1.1 服务器配置建议
```
基础配置（适合测试/小规模）：
- CPU：2核
- 内存：4GB
- 系统盘：40GB SSD
- 带宽：3Mbps
- 系统：Ubuntu 20.04 LTS
- 价格：约150元/月

生产配置（适合正式商用）：
- CPU：4核
- 内存：8GB
- 系统盘：100GB SSD
- 带宽：5Mbps
- 系统：Ubuntu 20.04 LTS
- 价格：约400元/月
```

#### 1.2 购买步骤
```
1. 访问阿里云官网（aliyun.com）
2. 注册/登录账号
3. 进入"云服务器ECS"产品页
4. 点击"立即购买"
5. 选择配置：
   - 计费方式：包年包月（更便宜）
   - 地域：选择离用户近的地区（如华东1）
   - 实例：共享标准型 s6
   - 镜像：Ubuntu 20.04 64位
   - 存储：40GB高效云盘
   - 网络：按流量计费，带宽峰值5Mbps
   - 安全组：开放80、443、22端口
6. 设置root密码（记住！）
7. 完成支付
```

### 第二步：连接服务器（30分钟）

#### 2.1 获取服务器信息
```
登录阿里云控制台 → 云服务器ECS → 实例
找到您的服务器，记录：
- 公网IP地址：例如 47.100.xxx.xxx
- 用户名：root
- 密码：您设置的密码
```

#### 2.2 使用SSH连接
**Windows用户**：
1. 下载安装 [PuTTY](https://www.putty.org/)
2. 打开PuTTY，输入：
   - Host Name: root@您的公网IP
   - Port: 22
   - Connection type: SSH
3. 点击Open，输入密码

**Mac/Linux用户**：
```bash
ssh root@您的公网IP
# 输入密码
```

#### 2.3 首次登录配置
```bash
# 更新系统
apt update && apt upgrade -y

# 安装必要工具
apt install -y curl wget vim git

# 设置时区（亚洲/上海）
timedatectl set-timezone Asia/Shanghai

# 创建部署目录
mkdir -p /var/www/smart-construction
cd /var/www/smart-construction
```

### 第三步：安装Web服务器（30分钟）

#### 3.1 安装Nginx
```bash
# 安装Nginx
apt install -y nginx

# 启动Nginx
systemctl start nginx
systemctl enable nginx

# 检查状态
systemctl status nginx

# 测试访问
# 在浏览器访问：http://您的公网IP
# 应该看到Nginx欢迎页面
```

#### 3.2 配置Nginx
```bash
# 备份默认配置
cp /etc/nginx/sites-available/default /etc/nginx/sites-available/default.backup

# 编辑配置文件
vim /etc/nginx/sites-available/default
```

将配置文件内容替换为：
```nginx
server {
    listen 80;
    server_name _;  # 暂时用IP访问，后面绑定域名
    root /var/www/smart-construction;
    index index.html index.htm;

    # 启用gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # 静态文件缓存
    location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 处理单页应用路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
    }
}
```

```bash
# 测试配置
nginx -t

# 重新加载配置
systemctl reload nginx
```

### 第四步：上传网站文件（1小时）

#### 4.1 方法一：使用SCP上传（推荐）
**Windows用户**：
1. 下载安装 [WinSCP](https://winscp.net/)
2. 打开WinSCP，输入：
   - 主机名：您的公网IP
   - 用户名：root
   - 密码：您的密码
   - 端口：22
3. 连接后，将本地文件拖到右侧服务器目录 `/var/www/smart-construction`

**Mac/Linux用户**：
```bash
# 在本地电脑执行
scp -r /本地/智慧工地平台/* root@您的公网IP:/var/www/smart-construction/
```

#### 4.2 方法二：使用Git（需要Git仓库）
```bash
# 在服务器上
cd /var/www/smart-construction

# 克隆您的代码仓库
git clone https://github.com/您的用户名/智慧工地平台.git .

# 或者使用我的示例（需要先上传到GitHub）
```

#### 4.3 方法三：使用FTP
```bash
# 在服务器安装vsftpd
apt install -y vsftpd

# 配置vsftpd
vim /etc/vsftpd.conf
# 修改以下配置：
# anonymous_enable=NO
# local_enable=YES
# write_enable=YES
# chroot_local_user=YES

# 重启服务
systemctl restart vsftpd

# 创建FTP用户
adduser ftpuser
# 设置密码
passwd ftpuser

# 设置目录权限
chown -R ftpuser:ftpuser /var/www/smart-construction
```

### 第五步：配置后端API（2小时）

由于当前只有前端代码，需要添加后端支持：

#### 5.1 安装Node.js环境
```bash
# 安装Node.js 18.x
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# 验证安装
node --version  # 应该显示 v18.x.x
npm --version   # 应该显示 8.x.x
```

#### 5.2 创建简易后端
```bash
# 创建后端目录
mkdir -p /var/www/smart-construction-api
cd /var/www/smart-construction-api

# 初始化项目
npm init -y

# 安装依赖
npm install express cors body-parser jsonwebtoken bcryptjs mysql2

# 创建app.js
vim app.js
```

`app.js` 内容：
```javascript
const express = require('express');
const cors = require('cors');
const bodyParser = require('body-parser');
const jwt = require('jsonwebtoken');
const bcrypt = require('bcryptjs');

const app = express();
const PORT = 3000;
const SECRET_KEY = 'your-secret-key-change-this';

// 中间件
app.use(cors());
app.use(bodyParser.json());

// 模拟用户数据库
const users = [
    {
        id: 1,
        username: 'admin',
        password: '$2a$10$YourHashedPasswordHere', // 123456
        role: 'admin'
    }
];

// 登录接口
app.post('/api/login', async (req, res) => {
    const { username, password } = req.body;
    
    // 模拟数据库查询
    const user = users.find(u => u.username === username);
    
    if (!user) {
        return res.status(401).json({ error: '用户不存在' });
    }
    
    // 模拟密码验证（实际应该用bcrypt.compare）
    if (password !== '123456') {
        return res.status(401).json({ error: '密码错误' });
    }
    
    // 生成token
    const token = jwt.sign(
        { userId: user.id, username: user.username, role: user.role },
        SECRET_KEY,
        { expiresIn: '24h' }
    );
    
    res.json({
        success: true,
        token,
        user: {
            id: user.id,
            username: user.username,
            role: user.role
        }
    });
});

// 验证token中间件
const authenticateToken = (req, res, next) => {
    const authHeader = req.headers['authorization'];
    const token = authHeader && authHeader.split(' ')[1];
    
    if (!token) {
        return res.status(401).json({ error: '需要认证' });
    }
    
    jwt.verify(token, SECRET_KEY, (err, user) => {
        if (err) {
            return res.status(403).json({ error: '认证失败' });
        }
        req.user = user;
        next();
    });
};

// 受保护的路由示例
app.get('/api/projects', authenticateToken, (req, res) => {
    res.json({
        success: true,
        data: [
            { id: 1, name: '项目A', progress: 65 },
            { id: 2, name: '项目B', progress: 30 },
            { id: 3, name: '项目C', progress: 85 }
        ]
    });
});

// 启动服务器
app.listen(PORT, () => {
    console.log(`API服务器运行在 http://localhost:${PORT}`);
});
```

#### 5.3 配置PM2进程管理
```bash
# 安装PM2
npm install -g pm2

# 启动API服务
cd /var/www/smart-construction-api
pm2 start app.js --name "smart-construction-api"

# 设置开机自启
pm2 startup
pm2 save

# 查看状态
pm2 status
```

#### 5.4 配置Nginx反向代理
```bash
# 编辑Nginx配置
vim /etc/nginx/sites-available/default
```

在server块中添加：
```nginx
# API反向代理
location /api/ {
    proxy_pass http://localhost:3000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

```bash
# 重新加载Nginx
systemctl reload nginx
```

### 第六步：配置域名和SSL证书（1小时）

#### 6.1 购买域名
```
推荐域名注册商：
- 阿里云万网
- 腾讯云DNSPod
- Namecheap（国际）

域名建议：
- smartconstruction.cn
- zhihuigongdi.com
- jianzhugongdi.com

价格：50-100元/年
```

#### 6.2 配置DNS解析
```
在域名控制台添加A记录：
- 主机记录：@ 或 www
- 记录类型：A
- 记录值：您的服务器公网IP
- TTL：600秒

等待生效：5分钟-24小时
```

#### 6.3 安装SSL证书（免费）
```bash
# 安装Certbot
apt install -y certbot python3-certbot-nginx

# 获取证书
certbot --nginx -d 您的域名.com -d www.您的域名.com

# 按照提示操作：
# 1. 输入邮箱（用于证书到期提醒）
# 2. 同意服务条款
# 3. 选择是否接收邮件
# 4. 选择为所有域名配置HTTPS

# 测试自动续期
certbot renew --dry-run
```

#### 6.4 更新Nginx配置
Certbot会自动更新Nginx配置，支持HTTPS。

### 第七步：前端代码适配（1小时）

#### 7.1 修改API调用
编辑前端JavaScript文件，将API调用指向真实后端：

`js/login.js` 修改：
```javascript
// 修改登录函数
async function handleLogin(event) {
    event.preventDefault();
    
    const username = document.getElementById('username').value;
    const password = document.getElementById('password').value;
    const rememberMe = document.getElementById('rememberMe').checked;
    
    try {
        const response = await fetch('/api/login', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({ username, password })
        });
        
        const data = await response.json();
        
        if (data.success) {
            // 存储token和用户信息
            localStorage.setItem('token', data.token);
            localStorage.setItem('user', JSON.stringify(data.user));
            localStorage.setItem('isLoggedIn', 'true');
            localStorage.setItem('username', data.user.username);
            
            if (rememberMe) {
                localStorage.setItem('rememberMe', 'true');
            }
            
            // 跳转到工作台
            window.location.href = 'dashboard.html';
        } else {
            alert(data.error || '登录失败');
        }
    } catch (error) {
        console.error('登录错误:', error);
        alert('网络错误，请稍后重试');
    }
}
```

#### 7.2 添加API请求拦截器
创建 `js/api.js`：
```javascript
// API基础配置
const API_BASE_URL = ''; // 使用相对路径，由Nginx代理

// 获取token
function getToken() {
    return localStorage.getItem('token');
}

// 通用API请求函数
async function apiRequest(endpoint, options = {}) {
    const token = getToken();
    
    const defaultOptions = {
        headers: {
            'Content-Type': 'application/json',
            ...(token && { 'Authorization': `Bearer ${token}` })
        }
    };
    
    const mergedOptions = {
        ...defaultOptions,
        ...options,
        headers: {
            ...defaultOptions.headers,
            ...options.headers
        }
    };
    
    try {
        const response = await fetch(`${API_BASE_URL}${endpoint}`, mergedOptions);
        
        // 处理未授权
        if (response.status === 401) {
            localStorage.clear();
            window.location.href = 'login.html';
            throw new Error('未授权，请重新登录');
        }
        
        // 处理其他错误
        if (!response.ok) {
            throw new Error(`HTTP错误 ${response.status}`);
        }
        
        return await response.json();
    } catch (error) {
        console.error('API请求错误:', error);
        throw error;
    }
}

// 导出常用方法
export const api = {
    get: (endpoint) => apiRequest(endpoint, { method: 'GET' }),
    post: (endpoint, data) => apiRequest(endpoint, { 
        method: 'POST', 
        body: JSON.stringify(data) 
    }),
    put: (endpoint, data) => apiRequest(endpoint, { 
        method: 'PUT', 
        body: JSON.stringify(data) 
    }),
    delete: (endpoint) => apiRequest(endpoint, { method: 'DELETE' })
};
```

### 第八步：数据库配置（可选，2小时）

#### 8.1 安装MySQL
```bash
# 安装MySQL
apt install -y mysql-server

# 安全配置
mysql_secure_installation
# 按照提示设置root密码等

# 登录MySQL
mysql -u root -p
```

#### 8.2 创建数据库和用户
```sql
-- 创建数据库
CREATE DATABASE smart_construction DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户
CREATE USER 'smart_user'@'localhost' IDENTIFIED BY 'StrongPassword123!';

-- 授权
GRANT ALL PRIVILEGES ON smart_construction.* TO 'smart_user'@'localhost';

-- 刷新权限
FLUSH PRIVILEGES;

-- 退出
EXIT;
```

#### 8.3 创建数据表
```sql
-- 用户表
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    email VARCHAR(100),
    role ENUM('admin', 'manager', 'user') DEFAULT 'user',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 项目表
CREATE TABLE projects (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    start_date DATE,
    end_date DATE,
    progress INT DEFAULT 0,
    status ENUM('planning', 'in_progress', 'completed', 'cancelled') DEFAULT 'planning',
    created_by INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (created_by) REFERENCES users(id)
);

-- 更多表根据业务需求添加...
```

### 第九步：安全配置（1小时）

#### 9.1 防火墙配置
```bash
# 安装ufw
apt install -y ufw

# 配置规则
ufw default deny incoming
ufw default allow outgoing
ufw allow ssh
ufw allow http
ufw allow https

# 启用防火墙
ufw enable

# 查看状态
ufw status
```

#### 9.2 修改SSH端口
```bash
# 编辑SSH配置
vim /etc/ssh/sshd_config

# 修改以下行：
# Port 22 → Port 2222（或其他端口）
# PermitRootLogin yes → PermitRootLogin no
# PasswordAuthentication yes → PasswordAuthentication no（使用密钥）

# 重启SSH
systemctl restart sshd

# 注意：修改前确保新端口已开放
ufw allow 2222
```

#### 9.3 创建普通用户
```bash
# 创建新用户
adduser deploy

# 添加到sudo组
usermod -aG sudo deploy

# 切换到新用户
su - deploy
```

#### 9.4 设置自动备份
```bash
# 创建备份脚本
vim /usr/local/bin/backup.sh
```

```bash
#!/bin/bash
# 备份脚本
BACKUP_DIR="/backups"
DATE=$(date +%Y%m%d_%H%M%S)
DB_NAME="smart_construction"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
mysqldump -u smart_user -p'StrongPassword123!' $DB_NAME > $BACKUP_DIR/${DB_NAME}_${DATE}.sql

# 备份网站文件
tar -czf $BACKUP_DIR/website_${DATE}.tar.gz /var/www/smart-construction

# 删除7天前的备份
find $BACKUP_DIR -type f -mtime +7 -delete

echo "备份完成: $DATE"
```

```bash
# 设置权限
chmod +x /usr/local/bin/backup.sh

# 添加到crontab（每天凌晨2点备份）
crontab -e
# 添加：0 2 * * * /usr/local/bin/backup.sh
```

### 第十步：监控和维护（30分钟）

#### 10.1 安装监控工具
```bash
# 安装htop查看资源
apt install -y htop

# 安装日志查看工具
apt install -y logwatch

# 安装网站监控（可选）
# 使用UptimeRobot、Pingdom等第三方服务
```

#### 10.2 设置日志轮转
```bash
# 配置Nginx日志轮转
vim /etc/logrotate.d/nginx
```

```
/var/log/nginx/*.log {
    daily
    missingok
    rotate 14
    compress
    delaycompress
    notifempty
    create 0640 www-data adm
    sharedscripts
    postrotate
        [ -f /var/run/nginx.pid ] && kill -USR1 `cat /var/run/nginx.pid`
    endscript
}
```

#### 10.3 性能优化
```bash
# 优化Nginx配置
vim /etc/nginx/nginx.conf
```

调整以下参数：
```nginx
worker_processes auto;  # 自动根据CPU核心数设置
worker_connections 1024;  # 每个worker最大连接数
keepalive_timeout 65;  # 保持连接超时
client_max_body_size 100M;  # 最大上传文件大小
```

### 🌐 访问测试

#### 测试步骤：
1. **HTTP访问**：`http://您的公网IP`
2. **HTTPS访问**：`https://您的域名.com`
3. **功能测试**：
   - 登录功能（admin/123456）
   - 工作台加载
   - 模块切换
   - 数据展示

#### 常见问题解决：

**问题1：无法访问网站**
```bash
# 检查Nginx状态
systemctl status nginx

# 检查防火墙
ufw status

# 检查端口
netstat -tulpn | grep :80
netstat -tulpn | grep :443
```

**问题2：API接口报错**
```bash
# 检查PM2状态
pm2 status

# 查看API日志
pm2 logs smart-construction-api

# 检查Nginx代理配置
nginx -t
```

**问题3：数据库连接失败**
```bash
# 检查MySQL状态
systemctl status mysql

# 测试连接
mysql -u smart_user -p'StrongPassword123!' -e "SHOW DATABASES;"
```

### 📱 移动端访问优化

#### 响应式设计检查
确保网站在移动设备上正常显示：
1. 使用Chrome开发者工具模拟移动设备
2. 测试不同屏幕尺寸
3. 优化触摸交互

#### PWA支持（可选）
添加渐进式Web应用支持：
```html
<!-- 在index.html中添加 -->
<link rel="manifest" href="/manifest.json">
```

创建 `manifest.json`：
```json
{
  "name": "智慧工地平台",
  "short_name": "智慧工地",
  "description": "专业的智慧工地管理平台",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#007bff",
  "icons": [
    {
      "src": "/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

### 🚀 快速部署脚本

创建一键部署脚本 `deploy.sh`：
```bash
#!/bin/bash
# 智慧工地平台一键部署脚本

echo "开始部署智慧工地平台..."

# 1. 更新系统
echo "更新系统..."
apt update && apt upgrade -y

# 2. 安装Nginx
echo "安装Nginx..."
apt install -y nginx

# 3. 安装Node.js
echo "安装Node.js..."
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# 4. 安装MySQL
echo "安装MySQL..."
apt install -y mysql-server

# 5. 安装PM2
echo "安装PM2..."
npm install -g pm2

# 6. 配置防火墙
echo "配置防火墙..."
apt install -y ufw
ufw allow ssh
ufw allow http
ufw allow https
ufw --force enable

# 7. 创建目录结构
echo "创建目录..."
mkdir -p /var/www/smart-construction
mkdir -p /var/www/smart-construction-api

# 8. 复制文件（需要手动上传）
echo "请手动上传网站文件到 /var/www/smart-construction"
echo "请手动上传API文件到 /var/www/smart-construction-api"

# 9. 配置Nginx
echo "配置Nginx..."
cat > /etc/nginx/sites-available/default << 'EOF'
server {
    listen 80;
    server_name _;
    root /var/www/smart-construction;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
EOF

# 10. 重启服务
echo "重启服务..."
systemctl restart nginx
systemctl enable nginx

echo "部署完成！"
echo "请访问：http://$(curl -s ifconfig.me)"
```

### 📊 部署检查清单

- [ ] 服务器购买完成
- [ ] SSH连接成功
- [ ] Nginx安装配置
- [ ] 网站文件上传
- [ ] 域名解析生效
- [ ] SSL证书安装
- [ ] 后端API运行
- [ ] 数据库配置（可选）
- [ ] 防火墙配置
- [ ] 备份策略设置
- [ ] 监控工具安装
- [ ] 功能测试通过

### 💰 成本估算

#### 月度成本：
- 云服务器：150-400元
- 域名：4-8元（按年摊）
- SSL证书：0元（Let's Encrypt免费）
- 带宽费用：50-200元（按流量）
- **总计：200-600元/月**

#### 一次性成本：
- 域名注册：50-100元/年
- 备案服务（如需）：0-500元
- 开发时间：按团队成本计算

### 🆘 技术支持

#### 遇到问题怎么办？
1. **查看日志**：`tail -f /var/log/nginx/error.log`
2. **检查服务状态**：`systemctl status nginx`
3. **测试连接**：`curl -I http://localhost`
4. **搜索错误信息**：在搜索引擎中搜索具体错误
5. **寻求帮助**：Stack Overflow、服务器厂商技术支持

#### 紧急联系方式：
- 阿里云技术支持：95187
- 腾讯云技术支持：95716
- 服务器厂商工单系统

### 🎉 部署完成！

您的智慧工地平台现在已经可以通过互联网访问了！

**访问地址**：
- 临时访问：`http://您的公网IP`
- 正式访问：`https://您的域名.com`

**测试账号**：
- 用户名：admin
- 密码：123456

**下一步建议**：
1. 邀请团队成员测试
2. 收集使用反馈
3. 根据需求添加更多功能
4. 考虑数据迁移和备份策略
5. 制定维护计划

**需要我帮助解决任何部署问题，请随时告诉我！**